---
phase: 03-lint-cleanup
plan: 05
type: execute
wave: 1
depends_on: []
files_modified: []
autonomous: true
requirements: []
gap_closure: true
must_haves:
  truths:
    - "npm run lint проходит без ошибок (0 errors)"
    - "Нет неиспользуемых переменных"
  artifacts: []
  key_links: []
---

<objective>
Финальное исправление 182 ошибок lint для достижения цели "npm run lint = 0 errors".

Проблема: Большинство ошибок - неиспользуемые переменные, которые можно исправить добавлением _ префикса.
</objective>

<context>
# Текущее состояние
- 182 errors, 121 warnings (после 03-04)
- Ошибки: unused-vars = 163, other = 19

# Ошибки по категориям
1. no-unused-vars: ~163 ошибок (префикс _ нужен)
2. Другие: ~19 ошибок

# Подход
- Запустить `npx eslint --fix` для авто-исправления
- Для оставшихся: добавить _ префикс к неиспользуемым параметрам
- Файлы: API routes, components, hooks, lib
</context>

<tasks>

<task type="auto">
  <name>Task 1: Авто-исправление eslint</name>
  <files>[]</files>
  <action>
Запустить `npx eslint --fix` для автоматического исправления:
1. Это должно исправить многие unused-vars ошибки
2. Проверить результат: npm run lint
  </action>
  <verify>`npm run lint 2>&1 | grep -c "error"`</verify>
  <done>Часть ошибок исправлена</done>
</task>

<task type="auto">
  <name>Task 2: Исправить оставшиеся unused-vars</name>
  <files>[]</files>
  <action>
Для оставшихся ошибок no-unused-vars:
1. Добавить _ префикс к неиспользуемым параметрам функций
2. Примеры: _userId, _error, _data, _params

Файлы с наибольшим кол-вом ошибок:
- src/app/api/*/route.ts
- src/app/components/*.tsx
</action>
  <verify>`npm run lint 2>&1 | grep -c "no-unused-vars"`</verify>
  <done>0 unused-vars ошибок</done>
</task>

<task type="auto">
  <name>Task 3: Исправить оставшиеся ошибки</name>
  <files>[]</files>
  <action>
Для оставшихся 19 ошибок:
1. Определить каждую ошибку
2. Исправить индивидуально
3. Запустить npm run lint для проверки
  </action>
  <verify>`npm run lint 2>&1 | tail -5`</verify>
  <done>0 errors</done>
</task>

<task type="auto">
  <name>Task 4: Финальная проверка</name>
  <files>[]</files>
  <action>
Запустить `npm run lint`:
- Должно быть 0 errors
- Предупреждения допустимы
  </action>
  <verify>`npm run lint`</verify>
  <done>npm run lint = 0 errors</done>
</task>

</tasks>

<verification>
`npm run lint` возвращает 0 errors
</verification>

<success_criteria>
- Достигнута цель: npm run lint = 0 errors
</success_criteria>

<output>
After completion, create `.planning/phases/03-lint-cleanup/{phase}-05-SUMMARY.md`
</output>
