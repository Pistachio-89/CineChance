---
phase: 07-admin-user-statistics
plan: '02'
type: execute
wave: 1
depends_on: []
files_modified:
  - src/app/admin/users/page.tsx
autonomous: true
requirements: []
must_haves:
  truths:
    - Admin can filter users by column values
    - Admin can sort by any column (asc/desc)
    - Filter and sort work together
  artifacts:
    - path: src/app/admin/users/page.tsx
      provides: User table with filter/sort
      contains: filter inputs per column, sort indicators
---

<objective>
Add column filtering and sorting to /admin/users table.

Purpose: Admins need to find specific users quickly - by name, email, role, date registered, etc.

Output: Table with filter inputs in headers and sort indicators on columns.
</objective>

<context>
@src/app/admin/users/page.tsx (after Plan 01)
</context>

<tasks>

<task type="auto">
  <name>Add column sorting</name>
  <files>src/app/admin/users/page.tsx</files>
  <action>
Add sorting to table columns:
1. Click column header to sort ascending
2. Click again to sort descending  
3. Show sort indicator (arrow up/down) on active column
4. Sortable columns: name, email, createdAt, role (if exists)
</action>
  <verify>Clicking column header sorts data, indicator shows sort direction</verify>
  <done>Table columns sortable with visual indicators</done>
</task>

<task type="auto">
  <name>Add column filtering</name>
  <files>src/app/admin/users/page.tsx</files>
  <action>
Add filter inputs to table:
1. Text input for name/email search
2. Dropdown for role filter (if users have roles)
3. Date range filter for registration date
4. Filters apply in real-time or on button click
</action>
  <verify>Filter inputs visible, typing filters the table</verify>
  <done>Table has working filter inputs for key columns</done>
</task>

</tasks>

<verification>
- [ ] Sort indicators visible on columns
- [ ] Clicking header sorts data
- [ ] Filter inputs present
- [ ] Filters reduce displayed rows correctly
- [ ] Sort + filter work together
</verification>

<success_criteria>
Admin can efficiently find users using filters and sorting.
</success_criteria>

<output>
After completion, create `.planning/phases/07-admin-user-statistics/07-02-SUMMARY.md`
</output>
